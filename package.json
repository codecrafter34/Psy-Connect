{
  "name": "psy-connect",
  "version": "1.0.0",
  "description": "Psy-Connect — Mental Health Platform",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"server,client,ml\" -c \"cyan,magenta,yellow\" \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:ml\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm run wait:server && npm --prefix client run dev",
    "dev:ml": "python ml-service/app.py",
    "wait:server": "wait-on http://localhost:5000/api/health",
    "install:all": "npm --prefix server install && npm --prefix client install"
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "wait-on": "^9.1.0"
  }
}
